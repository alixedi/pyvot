[project]
name = "pyvot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-fasthtml>=0.12.19",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]
